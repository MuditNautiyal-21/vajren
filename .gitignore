# weights and runtimes — huge, never commit
models/
llama/
*.gguf
*.onnx
*.bin
*.safetensors

# secrets
.env
*.key
*.pem
credentials.json
token.json

# personal — the writing samples the writer lane learns your voice from.
# Never commit these. config/voice.example.md is the template that ships.
config/voice.md

# runtime state
logs/
workspace/
sandbox/
memory/*.db
memory/*.db-wal
memory/*.db-shm
memory/index/
audit/

# python
__pycache__/
*.py[cod]
.venv/
env/
.pytest_cache/
.ipynb_checkpoints/

# os / editor
Thumbs.db
desktop.ini
.vscode/
.idea/
